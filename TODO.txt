Follow next poll data from feeds

https://github.com/3d0c/skeleton

go-spew for pprinting



Signal HUP reload
- if no longer exists in db:
- stop polling
- what happens when polling stops during a crawl? Does the channel still exist
  to pick up the response?
- On goroutine return objects should be freed?
